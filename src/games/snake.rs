// the snake game here 
// will be played on the oled inside the gamepad

use crate::display::OledDisplay;
use crate::Buttons;

pub fn start(display: &mut OledDisplay, buttons: &Buttons){
    display.clear();
    //TO DO
}